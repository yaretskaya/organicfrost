import { getProducts, addProduct, updateProduct, deleteProduct, Product, ProductData } from "./productData"

export { Product, ProductData }
export { getProducts, addProduct, updateProduct, deleteProduct }

